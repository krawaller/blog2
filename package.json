{
  "dependencies": {
    "next": "^9.0.3",
    "nprogress": "^0.2.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "front-matter": "^3.0.1",
    "fs-extra": "^7.0.1",
    "highlight.js": "^9.13.1",
    "inline-style-transformer": "^1.1.1",
    "marked": "^0.5.2"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  },
  "scripts": {
    "dev": "next",
    "build": "next build",
    "makePosts": "node makePosts",
    "start": "next start",
    "export": "next export && touch out/.nojekyll",
    "publish": "npm run export && cd out && git init && git add . && git commit -m 'rebuilt' && git remote add origin git@github.com:krawaller/krawaller.github.io.git && git push -f origin master",
    "test": "jest"
  }
}
